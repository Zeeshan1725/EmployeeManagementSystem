# EmployeeManagementSystem
This Employee Management System is a lightweight web application built with Django and SQLite. It streamlines HR tasks using MVT architecture to provide full CRUD capabilities—allowing administrators to easily add, view, update, and remove employee records via a simple interface without complex server setup.
